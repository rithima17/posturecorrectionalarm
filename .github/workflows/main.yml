name: Posture Correction CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    - name: Set up job
      run: echo "Job started"

    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Show repository structure
      run: ls

    - name: Setup Node
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Install Backend Dependencies
      run: echo "No backend dependencies"

    - name: Run Backend Tests
      run: echo "No tests"

    - name: Install Frontend Dependencies
      run: echo "Static project"

    - name: Build React App
      run: echo "No build needed"

    - name: Post Setup Node
      run: echo "Post setup done"

    - name: Complete job
      run: echo "Pipeline finished"
